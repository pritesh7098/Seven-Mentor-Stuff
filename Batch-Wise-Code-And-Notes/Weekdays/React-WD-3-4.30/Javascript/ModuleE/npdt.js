// name, age, address, isLogin

/* let name = "rahul"
const AGE = 24;
let isLogin = true;  */

// object creation

/* let userInfo = {
  // logic

  name1: "rahul",
  age: 24,
  address: "PCMC",
  isLogin: true,
};

console.log(
  `the user name is : ${userInfo.name1}`,
  `the user age is : ${userInfo.age}`,
);
 */

// Array Datatype in JS

/* let subjects = ["math", "cs", "history"]
 */

/* --------------------------------------------- */

// pass by value and pass by ref

/* let a = 10;
let b = a; // b = 10
a = 20; // b = 20

console.log(a); // 10 */

// a cannot be redeclare bt can be reassign

// pass by ref

/* let obj1 = { x: 1 };
let obj2 = obj1;

//console.log(obj2.x) 

obj2.x = 99; 

console.log(obj2.x)  */

/* ---------------------------------------------- */
